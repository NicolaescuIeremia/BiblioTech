name: C++ CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Compile
      run: g++ -std=c++17 src/*.cpp -o app

    - name: Run
      run: ./app